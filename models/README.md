# Trained model checkpoints

Place the trained model files in this directory using the names expected by the project:

- `resnet_model.keras`
- `vgg_model.keras`

For large checkpoint files, use Git LFS or GitHub Releases rather than normal Git tracking.
